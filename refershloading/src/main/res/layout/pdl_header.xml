<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@android:color/white"
    android:paddingBottom="@dimen/pdl_header_padding_bottom">

    <TextView
        android:id="@+id/pdl_tv_pull"
        android:layout_width="wrap_content"
        android:layout_height="@dimen/pdl_pull_tv_height"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true"
        android:drawableLeft="@mipmap/down"
        android:gravity="center_vertical"
        android:text="@string/pdl_pull"
        android:textColor="@android:color/black"
        android:textSize="@dimen/pdl_text_size" />

    <com.hsy.refershloading.view.GhostLoadingView
        android:id="@+id/pdl_glv_pull"
        android:layout_width="@dimen/pdl_loading_view_width"
        android:layout_height="@dimen/pdl_loading_view_height"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true"
        android:visibility="gone" />
</RelativeLayout>